# Copier vers .dev.vars (non versionné) pour tester le Worker en local avec wrangler dev.
# Production : définir les mêmes clés comme *Secrets* sur le worker bouffonsbios (dashboard Cloudflare).

PUBLIC_MAPBOX_ACCESS_TOKEN=

# Optionnel — notif Telegram (newsletter nouvelle inscription ; aligné avec les secrets Workers prod).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
